<main class="w-full m-3 flex flex-col gap-y-2">
  <%= link_to sanitize("&larr; Back to quote"), quote_path(@quote) %>

  <div class="text-2xl font-bold tracking-tight text-gray-900 sm:text-3xl">
    <h1>Edit quote</h1>
  </div>

  <%= render "form", quote: @quote %>
</main>

