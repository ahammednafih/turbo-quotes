<main class="w-full m-3 flex flex-col gap-y-2 scale">
  <%= link_to sanitize("&larr; Back to quotes"), quotes_path %>

  <div class="text-2xl font-bold tracking-tight text-gray-900 sm:text-3xl">
    <h1>New quote</h1>
  </div>

  <%= render "form", quote: @quote %>
</main>
